You can use prefixes to organize the data that you store in Amazon S3 buckets. A prefix is a string of characters at the beginning of the object key name. 
Use condition operators in the Condition element to match the condition key and value in the policy against values in the request context.
In the s3 condition only ec2 to list bucket with only object with prefix public-keys
